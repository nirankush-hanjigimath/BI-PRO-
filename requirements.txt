# Signal Engine — Final Locked Dependencies
# All pure-Python wheels, no C compiler required on Windows

# Core (already present)
requests>=2.31.0
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Signal engine additions
scipy>=1.11.0        # linear regression (np.polyfit used in ema_slope)
joblib>=1.3.0        # parallel symbol processing
schedule>=1.2.0      # live/paper loop scheduling
pyarrow>=14.0.0      # Parquet I/O for backtester historical data cache
